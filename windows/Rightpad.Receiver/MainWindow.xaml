<Window x:Class="Rightpad.Receiver.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Rightpad.Receiver"
    Title="rightpad Receiver" Style="{StaticResource {x:Type Window}}" Width="1100" Height="720" MinWidth="860" MinHeight="600" WindowStartupLocation="CenterScreen" SourceInitialized="WindowSourceInitialized" Loaded="WindowLoaded" Closing="WindowClosing">
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1" Padding="22,0">
      <DockPanel>
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
          <Ellipse Width="9" Height="9" Margin="0,0,9,0"><Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="{StaticResource MutedBrush}"/><Style.Triggers><DataTrigger Binding="{Binding Stats.Status}" Value="Connected"><Setter Property="Fill" Value="{StaticResource GreenBrush}"/></DataTrigger><DataTrigger Binding="{Binding Stats.Status}" Value="Disconnected"><Setter Property="Fill" Value="#E36B6B"/></DataTrigger><DataTrigger Binding="{Binding Stats.Status}" Value="Receiver Stopped"><Setter Property="Fill" Value="#7D8590"/></DataTrigger><DataTrigger Binding="{Binding Stats.Status}" Value="Error"><Setter Property="Fill" Value="#E99986"/></DataTrigger></Style.Triggers></Style></Ellipse.Style></Ellipse>
          <TextBlock Text="{Binding Stats.Status}" AutomationProperties.AutomationId="GlobalStatus"/>
        </StackPanel>
        <TextBlock FontSize="20"><Run FontWeight="SemiBold" Text="rightpad"/><Run Foreground="{StaticResource MutedBrush}" Text=" Receiver"/></TextBlock>
      </DockPanel>
    </Border>
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <Border Background="{StaticResource NavigationBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,1,0" Padding="10,20">
        <ListBox x:Name="Navigation" SelectedValuePath="Tag" SelectedValue="{Binding CurrentPage}" SelectionChanged="PageChanged" VerticalAlignment="Top" AutomationProperties.AutomationId="Navigation">
          <ListBoxItem Content="Overview" Tag="{x:Static local:ReceiverPage.Overview}"/>
          <ListBoxItem Content="Motion" Tag="{x:Static local:ReceiverPage.Motion}"/>
          <ListBoxItem Content="Tap" Tag="{x:Static local:ReceiverPage.Tap}"/>
          <ListBoxItem Content="Diagnostics" Tag="{x:Static local:ReceiverPage.Diagnostics}"/>
        </ListBox>
      </Border>
      <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24,22,24,20">
        <ContentControl x:Name="PageContent" HorizontalContentAlignment="Stretch"/>
      </ScrollViewer>
    </Grid>
    <StackPanel Grid.Row="2" Margin="204,0,24,12">
      <TextBlock Text="{Binding Stats.Error}" Foreground="#F0AA91" Style="{StaticResource OptionalNotice}"/>
      <TextBlock Text="{Binding Settings.Notice}" Style="{StaticResource OptionalNotice}" AutomationProperties.AutomationId="SettingsNotice"/>
    </StackPanel>
  </Grid>
</Window>
